# Conservative rustfmt configuration. Stable-only options so CI never
# needs `cargo +nightly fmt`.
edition             = "2024"
max_width           = 100
use_small_heuristics = "Max"
newline_style       = "Unix"
